{
  "name": "serveaso-be",
  "private": true,
  "description": "Serveaso backend monorepo — payments, preferences, providers, coupons microservices",
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "dev": "concurrently -n payments,prefs,prov,coupons -c blue,magenta,green,yellow \"PORT=4100 npm run dev --workspace=services/payments\" \"PORT=3001 npm run dev --workspace=services/preferences\" \"PORT=4000 npm run dev --workspace=services/providers\" \"PORT=3002 npm run dev --workspace=services/coupons\"",
    "dev:payments": "npm run dev --workspace=services/payments",
    "dev:preferences": "npm run dev --workspace=services/preferences",
    "dev:providers": "npm run dev --workspace=services/providers",
    "dev:coupons": "npm run dev --workspace=services/coupons",
    "start:payments": "npm run prod --workspace=services/payments",
    "start:preferences": "npm run start --workspace=services/preferences",
    "start:providers": "npm run start --workspace=services/providers",
    "start:coupons": "npm run start --workspace=services/coupons"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
